
import { dump } from 'frontend/helpers/dump';
import { module, test } from 'qunit';

module('Unit | Helper | dump');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dump([42]);
  assert.ok(result);
});

